# CLAUDE.md

## 프로젝트 개요
Match-Log — 축구/풋살 팀 매치 기록 서비스 (선수단 관리, 경기 투표, 라인업, 전술, 피드백, 스탯 기록)
- 백엔드: [JAVA SPRING]
- DB: [MYSQL]

## 빌드 & 실행
- 빌드: ./gradlew build
- 테스트: ./gradlew test
- 로컬 실행: ./gradlew bootRun

## 문서 위치
- API 명세: docs/api/
- DB 스키마: docs/db/schema.dbml

## 코드 설계 원칙
- 객체지향 SOLID 원칙을 위배하지 않는 방향으로 개발할 것
  - SRP(단일 책임): 클래스/메서드는 변경 이유가 하나여야 함. Service가 여러 도메인 책임을 떠안기 시작하면 분리할 것
  - OCP(개방-폐쇄): 새로운 케이스 추가 시 기존 코드를 수정하는 대신 확장으로 대응할 것 (예: `ErrorCode` 인터페이스 + 도메인별 enum 구현 패턴)
  - LSP(리스코프 치환): 하위 타입/구현체는 상위 타입 계약을 어기지 않고 치환 가능해야 함
  - ISP(인터페이스 분리): 클라이언트가 쓰지 않는 메서드에 의존하도록 인터페이스를 비대하게 만들지 말 것
  - DIP(의존관계 역전): 상위 계층(Controller/Service)은 구체 구현이 아닌 추상화에 의존할 것 (Service는 Repository 인터페이스에만 의존 — Spring Data JPA 구조상 기본적으로 지켜짐)
- 원칙 준수와 과도한 추상화 사이에서, 이 프로젝트는 3줄이면 되는 로직에 미리 인터페이스/전략 패턴을 씌우는 과설계보다 SRP/DIP를 우선 지키는 쪽을 기본으로 함

## 커밋 규칙

| 커밋 유형 | 의미 |
|---|---|
| feat | 새로운 기능 추가 |
| fix | 버그 수정 |
| docs | 문서 수정 |
| style | 코드 formatting, 세미콜론 누락 등 코드 자체의 변경이 없는 경우 |
| refactor | 코드 리팩토링 |
| test | 테스트 코드, 리팩토링 테스트 코드 추가 |
| chore | 패키지 매니저 수정, 그 외 기타 수정 (ex. .gitignore) |
| design | CSS 등 사용자 UI 디자인 변경 |
| comment | 필요한 주석 추가 및 변경 |
| rename | 파일 또는 폴더명을 수정하거나 옮기는 작업만인 경우 |
| remove | 파일을 삭제하는 작업만 수행한 경우 |
| !BREAKING CHANGE | 커다란 API 변경의 경우 |
| !HOTFIX | 급하게 치명적인 버그를 고쳐야 하는 경우 |

- 커밋 메시지는 위 유형 중 하나로 시작할 것 (예: `feat: 로그인 API 추가`)
- 커밋 하나에는 하나의 유형/목적만 담을 것
